# Config Repo

This repo mirrors dotfiles and tool configs from the home directory. When you change any files under `~/.claude`, `~/.codex`, or `~/.config`, apply the same changes to the corresponding paths in this repo so they stay in sync.
