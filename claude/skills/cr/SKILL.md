---
name: cr
description: Shorthand for /codex-review. Use when the user types "/cr" to get a code review.
allowed-tools: Bash, Read, Grep, Glob, Agent, Skill
---

# /cr — Code Review shortcut

This is an alias for `/codex-review`. Invoke it now:

Use the Skill tool to invoke `codex-review` with the same context the user provided.
