Commit message reminder: prefer not to include "claude code" or "hapi" in commit messages.

When ready to commit and push, use the /git-push skill — it handles committing, pushing, and optional PR creation from staged changes.
