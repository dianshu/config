Commit message reminder: prefer not to include "claude code" or "hapi" in commit messages.

When ready to commit and push, use the /create-pr skill — it handles branch creation, committing, pushing, and PR creation from staged changes.
