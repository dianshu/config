When a change affects user-visible behavior — CLI interface, installation steps, configuration, public APIs, or documented features — update README.md (or the project's equivalent user-facing doc) in the same change.

Skip for internal refactors, bug fixes that don't change documented behavior, test-only changes, and pure config tweaks.
