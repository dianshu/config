## Plan Pre-Review

Before presenting a plan for user approval (i.e., before calling ExitPlanMode), run `/codex-review` and `/gemini-review` on the plan file in parallel to get independent AI reviews. If the reviews identify issues, update the plan to address them before proceeding to user review.
