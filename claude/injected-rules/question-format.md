## 向用户提问的格式

需要用户在多个方向中做选择时：

1. 优先使用 `AskUserQuestion` 工具。
2. 不适用时，给每个选项加一个简短编号或标签（数字、字母、关键词均可，选最贴合语义的），方便用户直接回复编号。即使只有两个选项也加。
3. 当某个选项是你的推荐时，把它放在 **#1**，且 label 必须**正向**陈述该推荐动作（不能写"不做 X"再让用户选 #2 才执行 X）。这样"用户随手回 1"就是默认推荐路径。
