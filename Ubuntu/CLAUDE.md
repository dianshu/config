The @config repo mirrors dotfiles and tool configs from the home directory. When you change any files under `~/.claude`, `~/.codex`, or `~/.config`, apply the same changes to the corresponding paths in @config repo so they stay in sync.

This directory (`~/repos`) is a normal directory, not a git repo. It is used to hold git repos. Do not run git commands directly in this directory.
