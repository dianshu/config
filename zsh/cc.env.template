# Claude Code 环境变量
# 复制此文件到 ~/.config/claude/env 并填入实际值
# mkdir -p ~/.config/claude && cp cc.env.template ~/.config/claude/env

export BRAVE_API_KEY="your-brave-api-key"
export CONTEXT7_API_KEY="your-context7-api-key"
